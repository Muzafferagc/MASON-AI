# Cekirdek (Faz 1 + 1.5)
pywebview>=5.0
requests>=2.31

# Ses (Faz 2) - istege bagli: kurulmazsa Mason yine calisir, ses kapali olur
faster-whisper>=1.0
sounddevice>=0.4
numpy>=1.26
edge-tts>=6.1

# "Hey Mason" + sistem tepsisi (Faz 3) - istege bagli
pystray>=0.19
pillow>=10.0

# Gercek konusma-bitti algisi (Faz 9, Silero VAD) - istege bagli:
# kurulmazsa wakeword.py otomatik eski enerji-esigi yontemine doner.
silero-vad>=5.1
onnxruntime>=1.17

# Belge yukleme / RAG (Faz 4) - istege bagli: kurulmayan turler otomatik atlanir
# (PDF, Word, Excel okuma. Metin/kod/CSV ve gorsel/ses icin ek paket gerekmez;
#  gorsel Gemini goru ile, ses faster-whisper ile okunur.)
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1

# Bildirimler (Faz 5) - istege bagli: kurulmazsa tepsi bildirimi kullanilir
# Windows yerel toast bildirimleri icin.
plyer>=2.1
# Not: hava durumu (Open-Meteo) ve sabah brifingi ek paket gerektirmez;
# yalnizca 'requests' kullanir (zaten kurulu).
